# Spec — Distill / polish TUI (trilha C)

Data: 2026-07-17 · Base: `origin/master` pós-PR #11  
Independente da trilha B (PR #12).

## Escopo

| Item | Mudança |
| --- | --- |
| C1 Config labels | Rótulos humanos (`Provedores`, `Ordem`, …); chave técnica na dica |
| C2 Sidebar estreita | `≡` / `→` / `⚙` em vez de H/L/C |
| C3 Help discoverability | Chip `? ajuda` no Detail + hint `? ajuda` na borda inferior |
| C4 Chart estreito | `Min(6)` quando largura do painel &lt; 72; senão `Min(9)` |

## Não-objetivos

Contraste/tokens dual (trilha B). Novos atalhos. Redesign de layout.
