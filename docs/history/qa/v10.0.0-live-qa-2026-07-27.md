# Agent Bar v10 Live Omarchy QA Report — post-release 2026-07-27

> Historical snapshot (2026-07-27) of the v10.0.0 live QA. Known-residual
> item 1 (`appliedSettings` not loaded until Settings open) was fixed on
> 2026-08-01 (commit f48f233, requirement SET-023).

## Identity

```text
Date: 2026-07-27
Worker: Grok (post-merge release owner)
Repository: othavi0/agent-bar
Release tag: v10.0.0
Release commit: b9475bfc7b98817b717c148a193c8f31a07d43e8
Published archive SHA-256:
  102035312ef33c98498f645877ab0857a9eff39790d3a6a295c70276d786c6d6
Evidence root (full host dump): /tmp/agent-bar-v10-live-qa-2026-07-27/
In-repo report: docs/qa/v10.0.0-live-qa-2026-07-27.md
In-repo screenshots: docs/qa/v10.0.0-2026-07-27/
Authorization: User authorized live QA after dual RC + tag/publish.
Host: dual monitor Hyprland 0.56 / Omarchy Quattro
  eDP-1 1920x1080 @ (0,0)
  HDMI-A-1 1920x1080 @ (1920,0)
```

## Result summary

```text
Status: DONE_WITH_CONCERNS

Published v10.0.0 install, dual-monitor chips, popup open (left+right),
settings rail, provider view with Amp usage track, cross-monitor transfer,
outside-click dismiss, standard + purge uninstall (non-TTY JSON), and
exact baseline hash restore all evidenced.

Concerns (non-blocking for first tag; document honestly):
1) update check returns HTTP 403 against unauthenticated GitHub API from this
   host (rate limit). curl with gh token gets 200. Product path does not send
   token — env residual, not UI regression.
2) Theme dark/light probe not re-run (time-boxed optional).
3) Keyboard tour partial (Escape used; full rail focus matrix not exhaustive).
4) Uninstall residualPaths still report v9 waybar leftovers (owned-path policy;
   doctor clean scope separate).
5) Final live desktop left on published b9475bf after hash-proved restore.
```

## Acceptance matrix

| ID | Check | Result | Evidence |
| --- | --- | --- | --- |
| TEST-035 | Backup plugin/settings/shell/state hashed | **Pass** | `backup-baseline/`, `hashes/live-plugin-before.sha256`, shell/settings before |
| TEST-036 | Install published bundle transactionally | **Pass** | `steps/02-install.txt`; `install.sh --force --yes`; receipt `b9475bf…`; shell.json **bytes unchanged** |
| TEST-037 | Dual-monitor chips with % / state cues | **Pass** | `docs/qa/v10.0.0-2026-07-27/11-bar-eDP.png`, `12-chips-*.png`: Claude/Codex `— !`, Amp `100%`, Grok `85%` |
| TEST-037 | Popup open (pointer) | **Pass** | `docs/qa/v10.0.0-2026-07-27/{30-right-amp,50-settings-eDP,55-left-click-eDP}.png` |
| TEST-037 | Provider view + usage track | **Pass** | `docs/qa/v10.0.0-2026-07-27/60-provider-grok.png` shows Amp Connected + Daily **100%** progress track |
| TEST-037 | Settings rail / settings view | **Pass** | `docs/qa/v10.0.0-2026-07-27/50-settings-eDP.png` providers On, Remaining, 60s, notifications |
| TEST-037 | Popup transfer dual monitor | **Pass** | `docs/qa/v10.0.0-2026-07-27/53-transfer-HDMI.png` Codex panel on HDMI after eDP open |
| TEST-037 | Outside-click dismiss | **Pass** | `docs/qa/v10.0.0-2026-07-27/61-after-outside-eDP.png` no panel |
| TEST-037 | Keyboard (Escape dismiss) | **Pass** (partial) | Escape used between trials; full a11y matrix not exhaustive |
| TEST-037 | Theme dark/light | **Skip** | Optional / time-boxed |
| TEST-037 | Provider states in UI | **Pass** | Claude unauth, Codex error+Retry, Amp ready 100%, Grok ready 85% |
| TEST-038 | update check | **Fail (env)** | `sanitized-logs/update-check*.stderr`: `releases API returned HTTP 403` without token; authenticated curl 200 |
| TEST-038 | standard uninstall non-TTY JSON | **Pass** | `steps/04-uninstall-standard.txt` + report `c187a30c…`; plugin disabled/removed after worker |
| TEST-038 | purge uninstall non-TTY JSON | **Pass** | `steps/06-uninstall-purge.txt` + report `1e2dade7…`; settings purged (quarantine then gone); shell entry cleared |
| TEST-039 | Sanitized logs + real screenshots | **Pass** | `sanitized-logs/status.sanitized.json`, `screenshots/**` |
| TEST-040 | Rollback / restore hashes | **Pass** | `steps/08-rollback-verify.txt`: plugin/shell/settings **MATCH** baseline |
| TEST-041 | Restore after mutations | **Pass** | Baseline restored then re-applied published for daily use (`steps/10-final-published-install.txt`) |
| TEST-042 | No unrelated mutation | **Pass** | `other-plugins-before/after.sha256` MATCH; shell placement hash stable across install |

## Install path exercised

```text
AGENT_BAR_VERSION=10.0.0 bash install.sh --force --yes
→ download published tar.zst + sha256
→ checksum OK (102035312ef33c…)
→ stage validate receipt/manifest/helper
→ atomic swap plugin root
→ existing shell entry → omarchy plugin rescan (never bar plugin add)
→ shell.json SHA unchanged:
   27c9314e8c3042bc13a4305be0d0a80315eb3f3c4fc5fc4a7bf068a80a7de7fc
```

Pre-QA live plugin was RC `0df2904` (pre-merge polish). Post-install receipt is release commit `b9475bf`.

## Status sample (sanitized)

```text
schemaVersion: 2, helperVersion: 10.0.0
claude: unauthenticated (authentication_required)
codex: provider_error (rate limits not available) + Retry
amp: ready daily remaining 100%
grok: ready context remaining ~84.7% → chip shows 85% (metric remaining)
Account labels redacted in sanitized-logs/status.sanitized.json
```

## Uninstall confirmation documents used

Standard:

```json
{"schemaVersion":1,"operation":"uninstall","confirmed":true,"purgeSettingsAndBackups":false}
```

Purge:

```json
{"schemaVersion":1,"operation":"uninstall","confirmed":true,"purgeSettingsAndBackups":true}
```

Both accepted via non-TTY stdin; maintenance handoff via `systemd-run --user`.
Reports under `$XDG_STATE_HOME/agent-bar/reports/`. Residual v9 waybar paths listed
but not deleted (ownership policy).

## Dual RC (release engineering, same session)

```text
SOURCE_COMMIT=b9475bfc7b98817b717c148a193c8f31a07d43e8
archive/sidecar/metadata: byte-identical across two builds
archive sha256: 102035312ef33c98498f645877ab0857a9eff39790d3a6a295c70276d786c6d6
GitHub Release: https://github.com/othavi0/agent-bar/releases/tag/v10.0.0
CI note: publish.yml failed verify step (sha256sum CWD); fixed in PR #26 (merged).
Assets attached from dual RC (manual upload after CI fail).
```

## Security / privacy

- [x] Status artifact redacted (no account labels)
- [x] No credentials/tokens in report body
- [x] Live mutations limited to agent-bar plugin, XDG agent-bar, shell.json lifecycle
- [x] Baseline restored with matching hashes before final published reinstall
- [x] Synthetic input via local `/dev/uinput` only (no remote)

## Known residuals (unchanged product follow-ups)

1. `appliedSettings` not loaded until Settings open (chip order/metric defaults).
2. IpcHandler `health`/`refresh` typed-arg warnings.
3. `update check` unauthenticated GitHub API 403 under rate limit.
4. Historical waybar residual paths reported on uninstall.

## Live final state

```text
plugin: agent-bar.usage 10.0.0 enabled
sourceCommit: b9475bfc7b98817b717c148a193c8f31a07d43e8
shell.json: unchanged hash 27c9314e…
settings: restored then preserved through final published install
other plugins: unchanged
```

## One-line standup

> Published v10.0.0 live-accepted on dual monitors: chips + popup tour + purge uninstall + hash restore green; update-check 403 is GitHub unauth rate-limit residual.
